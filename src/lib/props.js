import { writable } from "svelte/store";

export const rounded = writable(false);
export const animated = writable(false);
export const hoverEffect = writable(false);
export const infiniteLoad = writable(true);